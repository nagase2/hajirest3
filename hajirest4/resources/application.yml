spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:file:/tmp/customer3;AUTO_SERVER=true
    username: sa
    password:
  jpa:
    hibernate.ddl-auto: validate
    
    
thymeleaf.cache: false
# 
security.basic.enabled: false


# this is comment