spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:file:/tmp/customer3
    username: sa
    password:
  jpa:
    hibernate.ddl-auto: validate
spring.thymeleaf.cache: false
security.basic.enabled: false
